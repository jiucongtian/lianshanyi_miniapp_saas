---
alwaysApply: true
---

## 编码习惯
- 对于重复的逻辑，通过定义函数并调用的方式，不要重复的逻辑由重新写一段代码
- 有些跟具体业务无关的通用类，在工具类中进行定义
- 实现新的业务逻辑或者编写新代码之前先查看需要使用的数据是否已经获取过或者已经存在于局部变量或全局变量中，如果已经存在了就没有必要重新获取，简化逻辑，避免冗余代码
- pages中的页面，如果存在的需要传递的变量比较多，尽量统一在一个数据结构，把这些需要传递的变量都放在这个数据结构中，然后跳转或者传递数据都使用这个数据结构，避免变量散列各处
- 每次修改和新增云函数的时候要修改对应的接口描述文档，每次调用云函数接口之前也要先阅读接口文档，避免使用参数和解析层级错误

## 执行对话命令时的规则
- 在处理任务的时候不要调用工具自动部署云函数，如果涉及到云函数的部署，则在执行完其他命令后，提示我应该部署哪些云函数，我会按照你的提示进行手动操作

## mock数据的定义
每个模块单独一个文件，存储在mock目录中

## 限制
- 没有我的允许，不要执行npm run dev指令
- 不要创建用于测试的页面
- 如果需要部署云函数到云端的情况，不要直接部署，最后提醒我需要手动部署就可以

## 关于接口的说明

## 命令规范
所有API命名都是 模块名称Api.js

## 关于接口文档
当生成或修改API接口或者mock接口时，需要同步生成对应的API文档，API文档按照模块分不同的文件，每个文件命令按照API的命名规范，模块+API。API放到docs目录中，文档格式如下：

- 接口名称
描述接口主要是做什么用的，用简单的话描述，比如获取用户基本信息，更新用户基本信息
-接口地址
对应API文件的真实地址，比如/user/info
-请求方式
    - get
    - post
只允许使用这两种方式
- 功能说明
对接口功能的描述，可以使用mermaid，也可以使用其他方式
- 请求参数
以json格式先示例，然后按以下格式说明参数的用途

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| 任意参数 | any | 否 | 测试参数，函数会将接收到的所有参数打印到日志中 |

-返回数据
###成功响应

```json
{
  "success": true,
  "message": "测试云函数执行成功！",
  "timestamp": 1694678400000,
  "data": {
    "openid": "用户的openid",
    "appid": "小程序的appid", 
    "unionid": "用户的unionid",
    "receivedParams": {
      "传入的参数": "参数值"
    },
    "executionTime": "2023-09-14T08:00:00.000Z"
  }
}
```

### 返回字段说明

| 字段名                 | 类型       | 说明 |
|-----------------------|-----------|------|
| success               | boolean   | 执行是否成功 |
| message               | string    | 执行结果消息 |
| timestamp             | number    | 执行时间戳 |
| data                  | object    | 详细数据 |
| data.openid           | string    | 用户的微信openid |
| data.appid            | string    | 小程序的appid |
| data.unionid          | string    | 用户的微信unionid（可能为空） |
| data.receivedParams   | object    | 函数接收到的参数 |
| data.executionTime    | string    | 执行时间（ISO格式）           |
