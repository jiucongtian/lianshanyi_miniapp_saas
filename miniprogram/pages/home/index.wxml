<view class="page-container">
  <!-- 顶部标题 -->
  <view class="header">
  </view>

  <!-- 主内容卡片 -->
  <view class="main-card">
    <!-- Tab导航 -->
    <view class="tab-nav">
      <view 
        class="tab-item {{currentTab === 'wisdom' ? 'active' : ''}}"
        bindtap="onTabChange"
        data-tab="wisdom"
      >
        <text class="tab-text">智慧洞见</text>
      </view>
      <view 
        class="tab-item {{currentTab === 'daily' ? 'active' : ''}}"
        bindtap="onTabChange"
        data-tab="daily"
      >
        <text class="tab-text">每日愈见</text>
      </view>
    </view>

    <!-- 智慧洞见内容 -->
    <view class="tab-content {{currentTab === 'wisdom' ? 'show' : 'hide'}}">
      <!-- 卡片标题 -->
      <view class="card-title">
        <text class="title-main">智慧洞见</text>
        <text class="title-sub">Wisdom and Insight</text>
      </view>

    <!-- 圆形装饰图案 -->
    <view class="mandala-container">
      <view class="mandala">
        <!-- 最外层 -->
        <view class="ring ring-outer-1">
          <view class="pattern pattern-v" wx:for="{{outerPatterns}}" wx:key="index" style="transform: rotate({{index * 10}}deg);"></view>
        </view>
        <!-- 第三层 -->
        <view class="ring ring-outer-2">
          <view class="pattern pattern-v" wx:for="{{outerPatterns}}" wx:key="index" style="transform: rotate({{index * 10}}deg);"></view>
        </view>
        <!-- 第二层 -->
        <view class="ring ring-middle">
          <view class="pattern pattern-heart" wx:for="{{middlePatterns}}" wx:key="index" style="transform: rotate({{index * 22.5}}deg);"></view>
        </view>
        <!-- 最内层星形 -->
        <view class="ring ring-inner">
          <view class="pattern pattern-star"></view>
        </view>
      </view>
    </view>

    <!-- 提示文字 -->
    <view class="instruction-text">心中默念,思考你的问题:</view>
    <!-- 示例问题滚动 -->
    <view class="example-question-container">
      <view class="example-question-swiper">
        <view class="example-question-wrapper">
          <view wx:for="{{exampleQuestions}}" wx:key="index" class="example-question-item">
            <view class="example-question">{{item}}</view>
          </view>
          <!-- 复制第一项用于无缝循环 -->
          <view class="example-question-item">
            <view class="example-question">{{exampleQuestions[0]}}</view>
          </view>
        </view>
      </view>
    </view>
    <view class="prompt-text">智慧洞见之门已打开， 你准备好提问了吗？</view>

    <!-- 输入框 -->
    <view class="input-container">
      <input 
        class="question-input" 
        placeholder="说出你的问题" 
        value="{{question}}"
        bindinput="onQuestionInput"
        confirm-type="done"
        maxlength="50"
      />
    </view>

    <!-- 按钮 -->
    <view class="button-container">
      <button class="find-answer-btn" bindtap="onFindAnswer" disabled="{{isSearching}}">
        <text wx:if="{{!isSearching}}">寻找答案</text>
        <text wx:else>寻找中...</text>
      </button>
    </view>
    </view>

    <!-- 每日愈见内容 -->
    <view class="tab-content {{currentTab === 'daily' ? 'show' : 'hide'}}">
      <!-- 卡片标题 -->
      <view class="card-title">
        <text class="title-main">每日愈见</text>
        <text class="title-sub">Daily Healing</text>
      </view>

      <!-- 每日愈见入口 -->
      <view class="daily-content" bindtap="onNavigateToDailyInsight">
        <view class="daily-entry-card">
          <!-- 顶部装饰 -->
          <view class="daily-entry-decoration">
            <view class="decoration-circle circle-1"></view>
            <view class="decoration-circle circle-2"></view>
            <view class="decoration-circle circle-3"></view>
          </view>
          
          <!-- 主要内容 -->
          <view class="daily-entry-content">
            <view class="daily-entry-icon">🌸</view>
            <view class="daily-entry-text-wrapper">
              <text class="daily-entry-title">今日治愈</text>
              <text class="daily-entry-subtitle">获取今日的治愈与启发</text>
            </view>
          </view>
          
          <!-- 底部提示 -->
          <view class="daily-entry-footer">
            <text class="daily-entry-hint">点击进入</text>
            <view class="daily-entry-arrow">→</view>
          </view>
        </view>
      </view>
    </view>
  </view>

  <!-- 背景装饰 -->
  <view class="bg-decoration">
    <view class="star-trail trail-1"></view>
    <view class="star-trail trail-2"></view>
    <view class="star-trail trail-3"></view>
    <view class="star-trail trail-4"></view>
    <view class="star-trail trail-5"></view>
    <view class="star-trail trail-6"></view>
    <view class="star-trail trail-7"></view>
    <view class="star-trail trail-8"></view>
  </view>
</view>

