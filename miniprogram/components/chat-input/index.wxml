<!--components/chat-input/index.wxml-->
<view class="input-container {{focused ? 'focused' : ''}}">
  <view class="input-wrapper">
    <textarea
      class="input-field"
      value="{{innerValue}}"
      placeholder="{{placeholder}}"
      placeholder-class="input-placeholder"
      confirm-type="send"
      bindinput="onInput"
      bindfocus="onFocus"
      bindblur="onBlur"
      bindconfirm="onConfirm"
      maxlength="500"
      auto-height="{{true}}"
      show-confirm-size="{{false}}"
      disable-default-style="{{true}}"
      adjust-position="{{true}}"
      fixed="{{true}}"
    />
  </view>

  <view class="send-btn {{innerValue && !disabled ? 'active' : 'disabled'}}" bindtap="onSend">
    <text class="send-text">发送</text>
  </view>
</view>